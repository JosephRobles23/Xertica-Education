## Parent
None

## What to build
Initialize settings configuration (`config/settings.py`), domain schemas (`models/domain/`), and DTO payload models (`models/dto/`). Configure FastAPI setup (`apps/api/main.py`) with routers and DI dependencies.

## Acceptance criteria
- [x] `Settings` load environment variables correctly.
- [x] Domain and DTO models compile.
- [x] FastAPI boots without errors.

## Blocked by
None - can start immediately.
