## Parent
None

## What to build
Build backend API endpoints under `routers/learning_paths.py` supporting `POST /learning-paths` (create), `GET /learning-paths/{id}` (fetch), and `PATCH /learning-paths/{id}` (modify). Setup the `LearningPathService` mock to return static mockup arrays. Integrate Dashboard and Details pages.

## Acceptance criteria
- [x] CRUD endpoints return expected mock JSON payloads.
- [x] Frontend successfully resolves endpoints and renders learning path information on Dashboard and Details screens.

## Blocked by
- [Issue 02 (Infrastructure Tracer Bullet)](file:///Users/sebastianmoseres/Desktop/All%20Folders/Xertica/Xertica%20Education/xertica-education/docs/issues/issue-02-infrastructure-tracer.md)
